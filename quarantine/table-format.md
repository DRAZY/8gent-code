# table-format

Format 2D data as an aligned ASCII table for terminal output.

## Requirements
- table(rows: string[][], options?) -> string
- Column alignment: left, right, center
- Optional header row with separator
- Max column width with truncation
- Box-drawing characters option (unicode borders)

## Status

Quarantine - pending review.

## Location

`packages/tools/table-format.ts`
