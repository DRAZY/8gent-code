# spinner

Terminal spinner with multiple animation styles.

## Requirements
- Spinner class with start(text), stop(), succeed(text), fail(text)
- Multiple frames: dots, line, arc, bounce
- Update text while spinning
- Clears line on stop
- Returns elapsed time on stop

## Status

Quarantine - pending review.

## Location

`packages/tools/spinner.ts`
